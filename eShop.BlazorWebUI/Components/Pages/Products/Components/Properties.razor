@switch (Model.Type.Name)
{
   case "fruit": <FruitProperties Properties="Model.Properties"/> break;
   case "vegetable": break;
   case "berries": break;
}

@code {
   [Parameter] public ProductModel Model { get; set; } = new();
}